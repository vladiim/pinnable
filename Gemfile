source "https://rubygems.org"

# Specify your gem's dependencies in pinnable.gemspec.
gemspec

gem "puma"

gem "sqlite3"

gem "propshaft"

# Hotwire delivery for the widget (dummy host uses importmap, like a real Rails 8 app).
gem "importmap-rails"
gem "stimulus-rails"

# System tests (headless Chrome) prove the click-to-pin flow end to end.
gem "capybara"
gem "selenium-webdriver"

# Omakase Ruby styling [https://github.com/rails/rubocop-rails-omakase/]
gem "rubocop-rails-omakase", require: false

# Start debugger with binding.b [https://github.com/ruby/debug]
# gem "debug", ">= 1.0.0"
